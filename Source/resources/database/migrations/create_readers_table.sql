CREATE TABLE readers(id INT PRIMARY KEY AUTO_INCREMENT,email VARCHAR(800) UNIQUE,password VARCHAR(100) NOT NULL,user_name VARCHAR(250) NOT NULL,verified_id BOOLEAN DEFAULT FALSE,no_of_books_read INT DEFAULT 0,last_book_read_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP,gmail_id VARCHAR(500) NULL,gmail_password VARCHAR(500) NULL);