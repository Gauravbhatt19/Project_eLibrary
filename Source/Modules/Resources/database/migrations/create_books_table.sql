CREATE TABLE books(bid INT PRIMARY KEY AUTO_INCREMENT,book_name VARCHAR(250) NOT NULL,book_categories VARCHAR(500) NULL,author_name VARCHAR(150) NOT NULL,edition VARCHAR(150) NOT NULL,readers_id VARCHAR(500) NULL);